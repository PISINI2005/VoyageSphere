package com.cts.enums;

public enum PackageStatus {
    AVAILABLE,       // slots available for booking
    FULLY_BOOKED,    // no slots available
    CANCELLED,       // package cancelled
    INACTIVE         // not listed / disabled in system
}
